# KNN
Implements K-Nearest Neighbours for classifying animal types by analysing data, preprocessing, selecting an optimal K and distance metric, and training the model. Performance is evaluated using accuracy, precision, recall, and F1-score, along with visualizing decision boundaries.
